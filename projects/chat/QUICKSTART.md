# ⚡ Быстрый старт - 5 минут до работающего чата!

## Шаг 1: Firebase (2 минуты)

1. Откройте: https://console.firebase.google.com/
2. Создайте новый проект
3. Включите **Realtime Database** → "Начать в тестовом режиме"
4. В настройках проекта скопируйте конфигурацию

## Шаг 2: Конфигурация (1 минута)

Откройте `index.html` и найдите (строка ~480):

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY_HERE",
    // ... остальные поля
};
```

**ЗАМЕНИТЕ** на ваши данные из Firebase Console.

## Шаг 3: Правила безопасности (1 минута)

В Firebase Console → Realtime Database → Rules:

```json
{
  "rules": {
    "messages": {
      ".read": true,
      ".write": true
    },
    "users": {
      ".read": true,
      ".write": true
    }
  }
}
```

Нажмите **Publish**.

## Шаг 4: Деплой (1 минута)

```bash
git add index.html
git commit -m "Configure Firebase"
git push origin main
```

В Settings → Pages → включите GitHub Pages.

## ✅ Готово!

Откройте ваш сайт и тестируйте чат на разных устройствах!

---

## 🔥 Важные ссылки

- Полная инструкция: [FIREBASE_SETUP.md](FIREBASE_SETUP.md)
- Документация: [README.md](README.md)
- Firebase Console: https://console.firebase.google.com/

---

## 🐛 Проблемы?

**"Firebase not configured"**
→ Проверьте, что вы правильно вставили конфигурацию

**"Permission denied"**
→ Проверьте правила безопасности в Firebase Console

**Чат не работает**
→ Откройте Console (F12) и посмотрите ошибки

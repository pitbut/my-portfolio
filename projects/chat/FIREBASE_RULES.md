# 🔒 Правила безопасности Firebase для chat-millionairov

## ⚠️ ВАЖНО! Настройте эти правила ПРЯМО СЕЙЧАС!

1. Откройте: https://console.firebase.google.com/project/chat-millionairov/database/chat-millionairov-default-rtdb/rules
2. Скопируйте правила ниже
3. Вставьте вместо существующих правил
4. Нажмите **"Опубликовать"** (Publish)

---

## 📋 Правила безопасности (скопируйте и вставьте)

```json
{
  "rules": {
    "messages": {
      ".read": true,
      ".write": true,
      ".indexOn": ["timestamp"],
      "$messageId": {
        ".validate": "newData.hasChildren(['author', 'text', 'timestamp', 'isSystem']) && 
                      newData.child('author').isString() && 
                      newData.child('text').isString() && 
                      newData.child('text').val().length <= 1000 &&
                      newData.child('timestamp').isNumber() &&
                      newData.child('timestamp').val() <= now + 60000"
      }
    },
    "users": {
      ".read": true,
      "$userName": {
        ".write": "!data.exists() || 
                   (data.child('name').val() == $userName && 
                    newData.child('name').val() == $userName)",
        ".validate": "newData.hasChildren(['name', 'joinedAt', 'lastActive']) &&
                      newData.child('name').isString() &&
                      newData.child('name').val() == $userName &&
                      newData.child('joinedAt').isNumber() &&
                      newData.child('lastActive').isNumber()"
      }
    }
  }
}
```

---

## 🛡️ Что делают эти правила:

### Messages (Сообщения):
- ✅ Все могут читать сообщения
- ✅ Все могут писать сообщения
- ✅ Проверка обязательных полей (author, text, timestamp, isSystem)
- ✅ Ограничение длины сообщения (макс. 1000 символов)
- ✅ Проверка временной метки (не может быть в будущем больше чем на 1 минуту)

### Users (Пользователи):
- ✅ Все могут видеть список пользователей
- ✅ Пользователь может создать свою запись
- ✅ Пользователь может обновлять только свою запись
- ✅ Невозможно изменить чужую запись
- ✅ Проверка обязательных полей (name, joinedAt, lastActive)

---

## 🚀 Быстрая проверка

После настройки правил:

1. **Откройте чат** на вашем сайте
2. **Войдите под любым именем**
3. **Отправьте сообщение**
4. Если всё работает - правила настроены правильно! ✅

---

## 🐛 Если появляется ошибка "Permission denied"

1. Проверьте, что правила опубликованы в Firebase Console
2. Убедитесь, что Realtime Database включена
3. Попробуйте очистить кэш браузера (Ctrl+Shift+Del)
4. Проверьте консоль браузера (F12) для деталей ошибки

---

## 📊 Мониторинг использования

Проверить использование базы данных:
1. Firebase Console → Realtime Database
2. Вкладка "Использование" (Usage)
3. Смотрите графики использования и лимиты

**Бесплатный план:**
- ✅ 1 GB хранилища
- ✅ 10 GB/месяц трафика
- ✅ 100 одновременных подключений

---

## 🔧 Дополнительные настройки (опционально)

### Очистка старых сообщений (рекомендуется)

Чтобы не превысить лимиты, можно автоматически удалять старые сообщения:

1. Firebase Console → Functions
2. Установите Cloud Functions
3. Добавьте функцию очистки (см. документацию)

### Модерация контента

Для автоматической модерации можно добавить:
- Cloud Functions для проверки текста
- Фильтр плохих слов
- Ограничение частоты сообщений (rate limiting)

---

## ✅ Готово!

После настройки правил ваш чат полностью защищён и готов к использованию!

**Проект:** chat-millionairov  
**Регион:** europe-west1 (Европа)  
**URL:** https://chat-millionairov-default-rtdb.europe-west1.firebasedatabase.app

# 📊 Построитель графиков

Интерактивный инструмент для создания, редактирования и экспорта графиков.

## 🎯 Возможности

### Основные функции:
- ✅ Создание множественных кривых (до 10+)
- ✅ Ручное добавление точек кликом мыши
- ✅ Построение графиков по математическим формулам
- ✅ Редактирование точек (перемещение, удаление)
- ✅ Сглаживание кривых сплайн-интерполяцией
- ✅ Настройка стилей линий и маркеров
- ✅ Экспорт в PNG, PDF, SVG
- ✅ Печать графиков

### Настройки графика:
- Подписи осей X и Y
- Максимальные значения осей
- Параметры сетки (шаг, видимость)
- Цвет линий (8 цветов)
- Стиль линий (сплошная, штриховая, штрихпунктирная, точечная)
- Форма точек (круг, квадрат, треугольник, ромб, звезда, крест)

### Режимы работы:
1. **Добавить точку** - клик по графику добавляет новую точку
2. **Удалить точку** - клик удаляет ближайшую точку
3. **Переместить точку** - перетаскивание точек мышью

## 📋 Установка

### Требования:
```bash
Python 3.7+
matplotlib
numpy
scipy
tkinter (обычно входит в Python)
```

### Установка зависимостей:
```bash
pip install matplotlib numpy scipy
```

## 🚀 Запуск

```bash
python graph_builder.py
```

## 📖 Инструкция по использованию

### Быстрый старт:

1. **Создание кривых:**
   - Введите количество кривых (например: 2)
   - Нажмите "Создать кривые"

2. **Настройка графика:**
   - Задайте подписи осей
   - Установите максимальные значения
   - Настройте сетку
   - Нажмите "Применить настройки"

3. **Работа с кривой:**
   - Выберите кривую из списка
   - Задайте название, цвет, стиль
   - Нажмите "Применить к кривой"

4. **Добавление точек:**
   - Режим: "Добавить точку"
   - Кликайте по графику
   - Точки автоматически соединяются

5. **Построение по формуле:**
   - Введите формулу: `x**2`, `sin(x)`, `exp(x)`
   - Задайте количество точек
   - Нажмите "Построить по формуле"
   - Можете редактировать точки вручную

6. **Сглаживание:**
   - Выберите кривую
   - Нажмите "Сгладить кривую"

7. **Экспорт:**
   - "Сохранить как PNG" - для документов
   - "Сохранить как PDF" - для публикаций
   - "Сохранить как SVG" - векторная графика
   - "Распечатать" - на принтер

## 🎨 Примеры формул

```python
# Простые функции
x**2              # парабола
x**3 - 2*x + 1    # кубическая функция

# Тригонометрические
sin(x)            # синус
cos(x)            # косинус
tan(x)            # тангенс

# Экспоненциальные
exp(x)            # экспонента
exp(-x)           # затухающая экспонента
2**x              # степень двойки

# Логарифмические
log(x)            # натуральный логарифм
log10(x)          # десятичный логарифм

# Другие
sqrt(x)           # квадратный корень
abs(x - 5)        # модуль
1/x               # гипербола

# Комбинации
sin(x) * exp(-x/10)        # затухающая синусоида
x**2 - 3*x + 2             # парабола со сдвигом
sqrt(abs(x))               # корень из модуля
```

## 💡 Полезные советы

1. **Для точных графиков:**
   - Используйте больше точек при построении по формуле (100-200)
   - Корректируйте отдельные точки вручную

2. **Для презентаций:**
   - Увеличьте размер маркеров
   - Используйте контрастные цвета
   - Экспортируйте в PNG с высоким разрешением

3. **Для научных статей:**
   - Экспортируйте в PDF или SVG
   - Используйте сдержанные цвета
   - Добавьте подробные подписи осей

4. **Быстрое редактирование:**
   - Ctrl+колесо мыши - прокрутка панели настроек
   - Режим перемещения - быстрая корректировка

## 🐛 Решение проблем

**Программа не запускается:**
- Проверьте установку Python 3.7+
- Установите зависимости: `pip install matplotlib numpy scipy`

**Ошибка при построении формулы:**
- Используйте `x` как переменную
- Функции: `sin()`, `cos()`, `exp()`, `log()`, `sqrt()`
- Пример правильной формулы: `sin(x) + 2`

**Графики не сохраняются:**
- Проверьте права на запись в выбранную папку
- Убедитесь что путь не содержит спецсимволов

## 📄 Лицензия

Свободное использование для образовательных и коммерческих целей.

## 👨‍💻 Автор

Разработано Pit, 2024

---

**Удачи в построении графиков!** 📊✨
